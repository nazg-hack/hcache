sudo: required
language: generic
services:
 - docker
env:
  matrix:
   - HHVM_VERSION=4.0.0
   - HHVM_VERSION=4.0.1
   - HHVM_VERSION=4.0.2
   - HHVM_VERSION=4.0.3
   - HHVM_VERSION=4.1.0
   - HHVM_VERSION=latest
install:
 - docker pull hhvm/hhvm:$HHVM_VERSION
 - docker images
script:
 - docker run /bin/bash -c "apt-get update && apt-get -y upgrade"
 - docker run /bin/bash -c "apt-get install memcached"
 - docker run /bin/bash -c "apt-get install redis-server"
 - docker run --rm -w /var/source -v $(pwd):/var/source hhvm/hhvm:$HHVM_VERSION ./.travis.sh
